package com.example.zoommeeting.util;


public class PasswordEncoder {

    private static final int BCRYPT_STRENGTH = 12;

//    public static String encode(String plain) {
//        return new BCryptPasswordEncoder(BCRYPT_STRENGTH).encode(plain);
//    }
//
//    public static boolean isPasswordMatches(String plainPassword, String encodedPassword) {
//        return new BCryptPasswordEncoder(BCRYPT_STRENGTH).matches(plainPassword, encodedPassword);
//    }
}
